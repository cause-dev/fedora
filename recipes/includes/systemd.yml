type: systemd
system:
  enabled:
    - nightly-shutdown.timer
    - nightly-enforcer.service
  disabled:
    - system-flatpak-setup.timer
    - flatpak-system-update.timer
    - rpm-ostreed-automatic.timer

user:
  disabled:
    - user-flatpak-setup.timer
    - flatpak-user-update.timer
  enabled:
    - podman.socket
